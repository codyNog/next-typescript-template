import { {{name}}Action } from "@/actions/{{name}}";
import { {{ name }}ActionMock } from "@/actions/{{name}}/mock";

test("{{name}}Action", () => {
  // test code
  expect({{name}}Action).toBeTruthy();
  expect({{ name }}ActionMock.params).toBeTruthy();
})