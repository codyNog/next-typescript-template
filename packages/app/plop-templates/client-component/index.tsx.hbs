'use client';
import { use{{pascalCase name}} } from "@/components/{{pascalCase name}}/hooks";
import type { Props } from "@/components/{{pascalCase name}}/types";
import _styles from "@/components/{{pascalCase name}}/index.module.css";

export const {{pascalCase name}} = (props: Props) => {
  use{{pascalCase name}}(props);
  return <div>{{pascalCase name}}</div>
};
