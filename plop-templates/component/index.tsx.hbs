import { use{{pascalCase name}} } from "~/components/{{pascalCase name}}/hooks"

type Props = {

};

export const {{pascalCase name}} = ({}: Props): JSX.Element => {
    const {} = use{{pascalCase name}}();
return (
);
};