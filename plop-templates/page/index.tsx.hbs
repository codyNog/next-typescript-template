import { use{{pascalCase name}}Page } from "~/components/pages/{{pascalCase name}}/hooks";

export const {{pascalCase name}}Page = (): JSX.Element => {
    const {} = use{{pascalCase name}}Page();
return (
);
};