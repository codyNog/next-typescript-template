import { {{ pascalCase name }} } from "~/domain/entities/{{ pascalCase name }}";

/**
 * {{ pascalCase name }} 一件作成
 * @returns 新規作成 {{ pascalCase name }}
 **/
const create{{ pascalCase name }} = async ({{ lowerCase name }}: {{ pascalCase name }}): Promise<{{ pascalCase name }}> => {
  return {{ lowerCase name }};
};

export { create{{ pascalCase name }} };

if (process.env.NODE_ENV === "test" && import.meta.vitest) {
  describe.skip("repositories/{{ pascalCase name }}", () => {});
}
