type Props = {

};

// eslint-disable-next-line no-empty-pattern
export const {{pascalCase name}} = async ({}: Props): Promise<JSX.Element> => {
return <div>{{pascalCase name}}</div> // cspell-disable-line
};
