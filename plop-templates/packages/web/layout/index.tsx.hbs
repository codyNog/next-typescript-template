import _styles from "./index.module.css";

type Props = LayoutProps<"/[locale]/{{kebabCase name}}">;

const Layout = async ({ children, params }: Props) => {
  const { locale } = await params;

  return children;
};

export default Layout;
