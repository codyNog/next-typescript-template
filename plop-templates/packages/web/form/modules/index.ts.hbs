import type { {{pascalCase name}}FormValues } from "../types";

// fix this to convert form values to create params
const toCreateParams = (
  values: {{pascalCase name}}FormValues,
) => {
  return { ...values };
};

// fix this to convert form values to update params
const toUpdateParams = (
  values: {{pascalCase name}}FormValues,
) => {
  return {
    ...values
  }
};

// fix this to convert data to default values
const toDefaultValues = (): {{pascalCase name}}FormValues => {
  return {
    id: ""
  }
}

export const {{name}}FormModules = {
    toCreateParams,
    toUpdateParams,
    toDefaultValues
}

