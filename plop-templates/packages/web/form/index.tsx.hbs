'use client';
import { use{{pascalCase name}}Form } from "./hooks";
import type { Props } from "./types";
import styles from "./index.module.css";

/**
 * Form Component for create or update {{pascalCase name}}.
 * @param {Props} props - {@link Props}
 */
export const {{pascalCase name}}Form = (props: Props) => {
  const { form, action, fields, t: _, onChange} = use{{pascalCase name}}Form(props);
  return <form id={form.id} action={action} onChange={onChange} noValidate>
    {/* hidden input for id */}
    <input type="hidden" className={styles.secureHidden}
      key={fields.id.key}
      id={fields.id.id}
      name={fields.id.name}
     />
    <button type="submit">{{pascalCase name}}Form</button>
  </form>
};
